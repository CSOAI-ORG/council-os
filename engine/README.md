# engine — PRIVATE. The proprietary GSPC deterministic scoring + Ed25519 signing core.

This is where the IP and defensibility live. It is NOT open-sourced. The open
`/adapters` layer feeds it reference data; it produces the signed measurement the
`/publishers` attach. Three-state by construction (pass / fail / UNMEASURED);
no model judges another; every number recomputable from its rows. Signing key
never leaves its isolation boundary (estate ANVIL doctrine).
